---
{key: code-binding/annotation-roundtrip,summary: Bidirectional sync between DB codeLinks and @spec JSDoc annotations in source files including symbol-rename propagation.,status: draft,type: brief,parent: code-binding,glossary: [codeLink,spec-annotation],brief: {context: {problem: "codeLinks live in the card DB but developers read source code; without an @spec JSDoc tag in\nsource the binding is invisible to readers. Without a sync from source @spec back to DB\ncodeLinks the source can drift silently. Symbol renames or moves break codeLinks unless\nexplicitly propagated.\n",impact: [{statement: Without @spec tags developers cannot tell which contracts a function carries.},{statement: A symbol rename without sync silently breaks every codeLink targeting the old name.}]},scope: {goals: [{id: G-001,statement: Inject @spec card-key annotations from DB codeLinks into source files (additive by default).},{id: G-002,statement: Reconstruct DB codeLinks by reading @spec annotations in source.},{id: G-003,statement: Propagate gildash-detected symbol renames or moves into card codeLinks.}],non_goals: [{id: NG-001,statement: Detecting drift (delegated to analysis).},{id: NG-002,statement: Editing source semantics beyond annotation comments.}],assumptions: [{id: A-001,statement: Source files are TypeScript-compatible JSDoc carriers.,verification: Inspect spec-sync.ts annotation parsing.,reevaluate_when: A non-TS language is added to scope.}]},flow: [{id: S-H-01,kind: happy,given: A card with three codeLinks and source files lacking those annotations.,when: writeSpecAnnotations runs in default additive mode.,then: Three @spec annotations are inserted in the right files; no existing annotations are removed.,covers: [G-001]},{id: S-H-02,kind: happy,given: A source file containing two @spec annotations not present in the DB.,when: syncSpecAnnotations runs.,then: The two missing codeLinks are added to the corresponding card.,covers: [G-002]},{id: S-H-03,kind: happy,given: gildash reports that symbol foo moved from a.ts to b.ts.,when: syncSymbolChanges runs.,then: "The card codeLink updates from {file:a.ts,symbol:foo} to {file:b.ts,symbol:foo}.",covers: [G-003]},{id: S-F-01,kind: failure,given: A writeSpecAnnotations --prune call where the user has manually added an @spec for a card that no longer references the symbol.,when: writeSpecAnnotations runs.,then: The orphan annotation is removed; user is prompted to confirm before --prune.,covers: [G-001]}],design: {overview: "writeSpecAnnotations is additive by default — it only injects missing @spec tags. --prune\nremoves orphans (annotations whose matching codeLink no longer exists). syncSpecAnnotations\nis the reverse direction: parse source for @spec tags and reconstruct DB codeLinks.\nsyncSymbolChanges queries gildash for renames and moves and applies the diff to card codeLinks.\n",components: [{name: writeSpecAnnotations,responsibility: "Inject @spec tags from DB codeLinks (additive by default, --prune to remove orphans).",interacts_with: [syncSpecAnnotations]},{name: syncSpecAnnotations,responsibility: Read @spec tags from source and reconstruct DB codeLinks.,interacts_with: [writeSpecAnnotations]},{name: syncSymbolChanges,responsibility: Apply gildash-reported renames and moves to card codeLinks.,interacts_with: []}],data_flow: [],invariants: [{id: DI-001,statement: Default writeSpecAnnotations never removes existing annotations.},{id: DI-002,statement: "--prune is gated on user confirmation in CLI."}]},policy: [{id: R-001,subject: writeSpecAnnotations,keyword: MUST,predicate: behave additively unless --prune is explicitly passed.,governs: [S-H-01,S-F-01]},{id: R-002,subject: syncSymbolChanges,keyword: SHALL,predicate: only apply changes reported by gildash (no heuristic guesses).,governs: [S-H-03]}],external: [{id: C-001,statement: JSDoc tag conventions follow standard TypeScript JSDoc parsing.,reference: {title: TypeScript JSDoc Reference,locator: https://www.typescriptlang.org/docs/handbook/jsdoc-supported-types.html}}],compatibility: {guarantees: [{subject: Annotation tag format,version_range: 1.x,breaks_if: The @spec tag syntax changes incompatibly.}]},limits: [{id: KL-001,statement: Annotation injection is only safe for files where canonical formatting is acceptable; manual formatting outside JSDoc may shift.},{id: KL-002,statement: syncSymbolChanges only sees what gildash reports; out-of-band edits are invisible until a reindex.}],criteria: [{id: SC-001,type: binary,measure: {predicate: Default writeSpecAnnotations on a file with existing user annotations leaves them in place.,method: Integration test diffing source before and after.},verifies: [S-H-01]},{id: SC-002,type: binary,measure: {predicate: "--prune confirmation is required by the CLI.",method: CLI test asserting prompt.},verifies: [S-F-01]}],rationale: {alternatives: [{option: Source-only as source of truth (no DB codeLinks).,pros: [Single representation.],cons: [Loses queryability and breaks the source-of-truth role of cards.]},{option: Auto-prune on every annotate run.,pros: [Always clean.],cons: [Removes user-added annotations silently,undermining trust.]}],chosen: {option: Additive default with explicit --prune for cleanup.,reasoning: Trust requires that the tool never removes user content silently.},addresses: [KL-001,KL-002]}}}
---
