---
{key: card-lifecycle/mutation-workflows,summary: "Create, update, delete, rename, and bulk-create workflows that own atomic card mutations and their parent rules.",status: draft,type: brief,parent: card-lifecycle,glossary: [4-tier],brief: {context: {problem: "Card mutation flows must coordinate validation, parent-existence checks, file IO, DB writes,\nand changelog entries. A naive implementation either drops one step (leaving file and DB\ndiverged) or skips parent checks (creating orphans that break the four-tier hierarchy).\n",impact: [{statement: Orphan cards or stale DB rows force expensive validateCards repair sweeps and obscure user intent.},{statement: Silent rename failures break codeLink targets and downstream drift detection.}]},scope: {goals: [{id: G-001,statement: "Provide createCard, updateCard, deleteCard, renameCard, bulkCreateCards entrypoints with consistent error semantics."},{id: G-002,statement: Reject mutations that would violate the four-tier hierarchy or reference a non-existent parent.},{id: G-003,statement: "Cascade renames so foreign-key style references (parent, relations, cross_domain_dependencies, body mentions) update atomically."}],non_goals: [{id: NG-001,statement: Schema validation itself (delegated to card-model).},{id: NG-002,statement: Drift transitions on mutation (delegated to analysis).}],assumptions: [{id: A-001,statement: "All mutations go through ops/ entrypoints, not direct repository writes.",verification: Grep imports of card-repo.ts outside of src/ops.,reevaluate_when: A new caller imports a repository directly.}]},flow: [{id: S-H-01,kind: happy,given: A valid create input for a brief whose parent domain exists.,when: createCard runs.,then: "The card is validated, written to file, persisted to DB, and a changelog row is appended in a single safe-write boundary.",covers: [G-001,G-002]},{id: S-H-02,kind: happy,given: A renameCard call from old key to new key with downstream references in three other cards.,when: renameCard runs.,then: "Parent, relations, cross_domain_dependencies, and body mentions in the three referencing cards update atomically and the file moves to the new path.",covers: [G-003]},{id: S-F-01,kind: failure,given: A createCard input whose parent does not exist.,when: createCard runs.,then: "A ParentValidationError is thrown, no file or DB row is written.",covers: [G-002]},{id: S-F-02,kind: failure,given: A bulkCreateCards input where the third entry fails validation.,when: bulkCreateCards runs.,then: All preceding writes roll back and the result reports partial status with the failed key isolated.,covers: [G-001]}],design: {overview: "Each entry point composes validation (card-model), parent resolution (card-storage), the\nmutation itself (file plus DB plus changelog), and rollback on failure (safe-write). Bulk-create\nruns entries serially inside one transaction so a mid-batch failure rolls every prior entry back.\n",components: [{name: createCard,responsibility: "Validate input, check parent existence, persist file plus DB plus changelog atomically.",interacts_with: [updateCard,bulkCreateCards]},{name: updateCard,responsibility: "Apply field, patch, body, glossary, and tag updates with replace-namespace semantics on patch.",interacts_with: [createCard]},{name: renameCard,responsibility: "Atomic key rename with downstream FK-style cascade across parent, relations, and cross-domain deps.",interacts_with: [updateCard]},{name: deleteCard,responsibility: Remove card with optional cascade through children (--force).,interacts_with: []},{name: bulkCreateCards,responsibility: Serial create within a single rollback boundary returning per-entry status.,interacts_with: [createCard]}],data_flow: [],invariants: [{id: DI-001,statement: A failed mutation leaves no partial file or DB state.},{id: DI-002,statement: Rename cascade updates every reference visible from the storage layer in one transaction.}]},policy: [{id: R-001,subject: Every mutation entry point,keyword: MUST,predicate: validate input through card-model before invoking storage.,governs: [S-H-01,S-F-01]},{id: R-002,subject: bulkCreateCards,keyword: SHALL,predicate: roll back all prior entries when any single entry fails validation or persistence.,governs: [S-F-02]},{id: R-003,subject: renameCard,keyword: MUST,predicate: "cascade FK-style references across parent, relations, cross_domain_dependencies, and card bodies in the same transaction as the file move.",governs: [S-H-02]}],external: [{id: C-001,statement: Mutations participate in the safe-write rollback contract owned by card-lifecycle/status-and-safe-write.,reference: {title: brief card-lifecycle/status-and-safe-write,locator: card-lifecycle/status-and-safe-write}}],compatibility: {guarantees: [{subject: "createCard, updateCard, deleteCard, renameCard, bulkCreateCards public signatures",version_range: 1.x,breaks_if: A required input field is added without a default.}]},limits: [{id: KL-001,statement: Rename cascade only updates references stored in the DB; codeLink @spec annotations in source files are reconciled separately by spec annotate.},{id: KL-002,statement: bulkCreateCards is serial; concurrent bulk operations are not supported within one process.}],criteria: [{id: SC-001,type: binary,measure: {predicate: A failed mid-batch bulkCreateCards leaves zero new rows in the cards table.,method: Integration test injecting a validation failure on the third entry of a five-entry batch.},verifies: [S-F-02]},{id: SC-002,type: binary,measure: {predicate: renameCard updates every cross-domain dependency that previously named the old key.,method: Integration test creating a graph then renaming and asserting reference counts.},verifies: [S-H-02]},{id: SC-003,type: binary,measure: {predicate: createCard with non-existent parent throws and writes nothing.,method: Integration test asserting empty disk and DB after error.},verifies: [S-F-01]}],rationale: {alternatives: [{option: Direct repository access from CLI commands.,pros: [Less indirection.],cons: [Each command would re-implement validation,parent checks,and rollback,drifting over time.]},{option: Optimistic concurrency with retry instead of serial bulk.,pros: [Higher throughput on contention.],cons: [Adds complexity for negligible gain on a single-user CLI tool.]}],chosen: {option: Single ops/ entry per mutation with serial bulk and atomic rollback.,reasoning: Single-user CLI workloads benefit from simplicity; the rollback boundary is the load-bearing invariant for trust.},addresses: [KL-001,KL-002]}}}
---
