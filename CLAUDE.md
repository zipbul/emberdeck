# Emberdeck

Emberdeck은 카드를 통해 프로젝트의 모든 설계 지식을 표현하는 시스템이다.
카드가 source of truth이고, 코드는 카드에서 derive된다.
사용자는 emberdeck을 다른 소프트웨어 프로젝트에 적용하여, 카드만으로 시스템 전체를 표현하고 카드로부터 코드를 재생성할 수 있게 한다.

AI가 코드를 쓰는 시대에 설계 지식은 프롬프트와 머릿속에서 증발하고, 코드는 왜를 대변하지 못하며, 바인딩 없는 문서는 코드와 어긋나 거짓말한다 — 그래서 지식을 검증 가능하게 코드와 묶인 단일 source of truth(카드)에 담고, 코드는 거기서 재생성되는 파생물로 둔다.

카드는 5-tier다: vision · principle · domain · brief · spec.
spec만 코드와 연결된다(@spec 바인딩). 나머지(vision · principle · domain · brief)는 개념 · 정의 · 기획 · 정책 · 이야기이며 코드를 대변하지 않는다.

비전은 프로젝트가 이루려는 모습으로서, 모든 의도가 거슬러 올라가 마지막으로 닿는, 그 자체로 성립하는 근거다.
원칙은 프로젝트의 모든 결정이 가로질러 지켜야 하도록 스스로에게 세운 규범이다.
도메인은 프로젝트가 다루는 세계를 가른 부분으로서, 각자의 용어와 규칙을 가진 의미 단위다.
기획(brief)은 누구를 위해, 왜, 무엇을 이루려는가라는 의도를, 방법을 정하기 전에, 만들 대상으로 전환하는 활동이다.
스펙은 기획이 만들기로 한 것이 어떠해야 하는지를, 만들어진 것만 보고 맞는지 가릴 수 있게 빠짐없이 정해 둔 기준이다.
